import Layout from "@/components/layout/Layout";
export default function CmsPage({ overrideSlug }: { overrideSlug?: string }) { return <Layout><div className="container py-20 text-center text-muted-foreground">CMS page — placeholder</div></Layout>; }
